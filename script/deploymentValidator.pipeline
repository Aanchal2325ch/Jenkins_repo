@Library('shared-library') _
pipeline
{
  agent{
  node{
  label 'Slave1'
  }
  }
  stages
  {
	stage('Build')
	{
		steps
		{
		script
		{		
		pipelineStages.initStage("SharedLibrary")		
		}
		}
	}
  }
}
